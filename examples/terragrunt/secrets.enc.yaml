dockerhub:
    registry: registry-1.docker.io
    username: user
    accessToken: token
gitlab:
    registry: registry.gitlab.com
    username: user
    accessToken: token
public-ecr:
    registry: public.ecr.aws
